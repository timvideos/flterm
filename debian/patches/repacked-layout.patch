Description: We don't keep upstream's directory layout
Forwarded: not-needed

--- a/Makefile
+++ b/Makefile
@@ -6,7 +6,7 @@
 all: $(TARGETS)
 
 %: %.c
-	$(CC) -O2 -Wall -I../common -s -o $@ $<
+	$(CC) -O2 -Wall -I. -s -o $@ $<
 
 install: flterm
 	install -d $(PREFIX)/bin
